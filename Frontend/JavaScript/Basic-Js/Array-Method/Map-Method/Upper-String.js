//************************************ Convert strings to uppercase **************** */
let upperCase = ["peter", "parker", "Bruce", "Wayne", "Clark Kent", "Tony Stark"];
let result = upperCase.map(ele => ele.toUpperCase());
console.log(result);