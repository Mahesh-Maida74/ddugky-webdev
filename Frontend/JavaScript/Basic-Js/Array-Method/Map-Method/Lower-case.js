//***********************************  From an array of lowercase strings, return a new array with all strings in uppercase.  */

let upperCase = ['PETER', 'PARKER', 'BRUCE', 'WAYNE', 'CLARK KENT', 'TONY STARK'];
let result = upperCase.map(ele => ele.toLowerCase());
console.log(result);